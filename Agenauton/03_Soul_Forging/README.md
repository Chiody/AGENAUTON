# Soul Forging 「锻造」

# 架构生成与代码骨架

> *"在 Cursor 里启动锻造程序，让逻辑瞬间转化为可运行的代码。"*

---

## What is Soul Forging?

Soul Forging is the final step of the Agenauton Forging Trilogy. It takes your completed 26-Dimension Blueprint and transforms it into a runnable code skeleton — the **immortal backbone** of your Agent.

**锻造** 是铸造三部曲的最后一步。将填写完成的 26 维度蓝图喂给代码生成管线，利用强模型生成不死骨架。

---

## Core Principle / 核心原则

> **强模型定骨架，弱模型填代码。方向永远不会错。**

1. **Step 1:** 用最强的模型（o1/Claude 3.5 Sonnet）基于 26 维度生成详细的系统架构
2. **Step 2:** 将这份"高精骨架"输入给编程工具（Cursor/Windsurf/Cline）
3. **Step 3:** 后续具体的函数实现、UI 调整等，调用便宜的低成本模型完成

这就像建筑：承重墙必须由最好的工程师设计，但刷墙、装修可以交给普通工人。

---

## Files in This Directory

| File | Description |
|------|-------------|
| [`skeleton_generator.md`](./skeleton_generator.md) | 🏗️ 架构生成指令 — 将蓝图转化为系统架构的元指令 |
| [`cursor_rules.md`](./cursor_rules.md) | 🎯 Cursor IDE 集成规则 — 可直接使用的 .cursorrules |

---

## Quick Start / 快速开始

### Method 1: Feed to AI Directly 直接喂给 AI

将你填写好的 26 维度测绘表，配合以下元指令，发送给强模型：

```
你是一位顶级系统架构师。以下是一份基于 Agenauton 26 维度标准填写的
AI Agent 蓝图。请基于这份蓝图：

1. 生成完整的系统架构设计（包含 Mermaid 流程图）
2. 定义所有数据模型（Pydantic Models）
3. 设计 Agent 间的通信协议
4. 输出项目的目录结构
5. 为每个核心模块编写接口定义（不需要实现）

要求：
- 架构必须覆盖蓝图中所有已填写的维度
- 代码使用 Python + FastAPI
- 输出格式为可直接在 Cursor 中使用的代码文件

[在此粘贴你的 26 维度测绘表]
```

### Method 2: Use Cursor Rules 使用 Cursor 规则

```bash
# 将 Agenauton Cursor 规则复制到你的项目
cp cursor_rules/agenauton.cursorrules /your/project/.cursorrules
```

然后在 Cursor 中开始对话，AI 将自动遵循 Agenauton 标准进行架构设计。

---

## Architecture Output Example / 架构输出示例

一个典型的锻造输出应包含：

```
my_agent/
├── main.py                    # 入口文件
├── config.py                  # 配置管理（维度 01, 06）
├── models/
│   ├── schemas.py             # 数据模型（维度 04）
│   └── state.py               # 状态管理（维度 05）
├── agents/
│   ├── orchestrator.py        # 主编排器（维度 08）
│   ├── planner.py             # 规划 Agent（维度 09）
│   ├── executor.py            # 执行 Agent（维度 09）
│   └── reviewer.py            # 审查 Agent（维度 16）
├── tools/
│   └── tool_registry.py       # 工具注册表（维度 10）
├── retrieval/
│   └── rag_pipeline.py        # RAG 管线（维度 11）
├── prompts/
│   ├── system_prompt.py       # 系统提示词（维度 12, 14）
│   └── intent_router.py       # 意图路由（维度 13）
├── safety/
│   ├── moderation.py          # 内容审核（维度 18）
│   ├── hallucination_check.py # 幻觉检测（维度 17）
│   └── trust_boundary.py      # 信任边界（维度 19）
├── evolution/
│   ├── telemetry.py           # 遥测日志（维度 20）
│   └── drift_manager.py       # 知识漂移管理（维度 22）
└── tests/
    └── ...
```

---

*→ Back to [README](../README.md)*
